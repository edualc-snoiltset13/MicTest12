# ---------------------------------------------------------------------------
# MedSimQA backend runtime dependencies
# Pinned to compatible-release ranges so that patch-level security fixes are
# picked up automatically while minor/major upgrades stay deliberate.
# ---------------------------------------------------------------------------
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
pydantic~=2.9.0
pydantic-settings~=2.5.0
SQLAlchemy~=2.0.35
alembic~=1.13.3
python-multipart~=0.0.12
orjson~=3.10.7
httpx~=0.27.2
python-dateutil~=2.9.0
PyJWT~=2.9.0
passlib[bcrypt]~=1.7.4
jsonschema~=4.23.0
structlog~=24.4.0

# Optional PostgreSQL driver. SQLite is the default for local/dev/CI so this is
# safe to drop if libpq headers are unavailable on the build host.
psycopg[binary]~=3.2.2

# --- test / lint (installed via requirements-dev.txt in CI) ----------------
